# Behavioral Corpus

The first corpus will contain one small model per observed feature and preserve
the raw FlatZinc, OZN, standard output, standard error, exit status, and a
written expected-behavior note for the archived and official compilers.

Do not add modern MiniZinc compatibility models here until the historical
behavior has been recorded.
