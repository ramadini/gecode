#!/bin/sh

set -eu

SCRIPT_DIR=$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)
FZN=$(mktemp "${TMPDIR:-/tmp}/mzn-gstrings.XXXXXX.fzn")
trap 'rm -f "$FZN"' EXIT HUP INT TERM

"$SCRIPT_DIR/mzn2fzn-gstrings" "$@" -o "$FZN"
"$SCRIPT_DIR/fzn-gstrings" "$FZN"
